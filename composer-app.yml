version: '3'

services:

  application:
    image: ldynia/conteneraized-workshop:2.0
    build: .
    restart: always
    container_name: demo
    volumes:
      - ./app:/app
    environment:
      MAGIC: abbracadabbra
    command: bash scripts/startup.sh
